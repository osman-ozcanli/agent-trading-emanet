# Secrets — never commit. OKX keys live in ~/.okx/config.toml, outside this repo.
.env
*.key
config.local.*

# Journals ARE the evidence and are committed on purpose (logs/journal*.jsonl,
# decisions*.md, state*.json, archive/). Only the raw console stream is excluded.
logs/agent.out
# Backtest ham mum onbellegi (1,9 MB): kanit degil tureyen veri, `--refresh` ile
# yeniden uretilir. Rapor (logs/BACKTEST.md) kanittir ve commit edilir.
logs/backtest_cache.json

# Runtime
node_modules/
__pycache__/
.venv/
*.pyc
no-mcp.json

# OS / editor / stray files
Thumbs.db
.DS_Store
*.doc
*.png
*.jpeg

# Tool dirs — skills are reinstalled from skills-lock.json via `npx skills add okx/agent-skills`
.claude/
.agents/
logs/agent.live.out

# Sunum sablonu: ajan her turda bundan uretir, repoya girmez
EMANET-sunum.orig.pptx
*.tmp
